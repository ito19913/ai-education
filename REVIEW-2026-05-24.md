# REVIEW — Phase 3 中盤の独立レビュー（2026-05-24）

Phase 3 中盤の機能群（1 日 1 chat 永続化、ゆい対話アーカイブ、過去 chat 検索、セッション pause/resume、ending dialogue 等）が一段落したタイミングで、3 つの独立レビューを実施した記録。それぞれ別エージェントが [PHILOSOPHY.md](./PHILOSOPHY.md) / [TUTOR-ROLE.md](./TUTOR-ROLE.md) / [ARCHITECTURE.md](./ARCHITECTURE.md) と全 web/ ソースを読み、独自視点で評価。

将来このプロジェクトに戻ってくる時 / 新しい Claude セッションに引き継ぐ時、「Phase 3 中盤で何が指摘されたか」「次に何を直すべきか」の起点として使う。

---

## 共通指摘（3 つのレビューが揃って言ってる事 = 最重要）

**ARCHITECTURE.md と実装の乖離**: `ReflectionLog` / `LongTermGoal` / `WeeklyGoal` / `GoalReview` / `TutorHandoff` / `NodeComprehension` の 6 型は docs で定義されているが実装ゼロ。`tutor-mock.ts` の excavation state は「申し送りしておくよ」と発話するが派生先が存在しない（**仕様詐欺に近い**、コーチング #2）。

→ **Phase 3a の前に、空でも型と mock データを置くべき**（仕様レビュー #1、コーチング #2）

---

## 1. 仕様・アーキテクチャレビュー

### 整合性が崩れているところ

- **`SessionEndReason` の dead enum 値**: `"auto-idle" | "auto-day-change" | "browser-close"` を定義しているが、現行設計では発火しない（新方針: auto-pause のみ、browser-close は snapshot 保存）。`endSession` を呼ぶのは "manual" のみ。`SessionEndDialog` の `REASON_LABELS` も全部にラベル割当中。**dead code 約 30 行**。
- **`/tutor?ending=1` の入口**: `TutorWorkspace` で受けているが、`/learn` 側の遷移箇所を grep して再確認要。endingMode の入口が宙に浮いている可能性。
- **`Memo` 旧型**: types.ts で deprecated 表示しつつ `MOCK_MEMOS` が残り、6 ファイルがまだ参照。「統合済み」と言い切るには中途半端。

### 欠落

- **Phase 3 拡張型 5 個が docs 空中浮遊**: `ReflectionLog` / `LongTermGoal` / `WeeklyGoal` / `GoalReview` / `TutorHandoff` が types.ts に **1 つも実装されていない**。tutor-mock.ts の excavation 終了時に「課題に追加 + 葵への申し送りしておくよ」と発話するが**実際には何も派生しない**。
- **`NodeComprehension`**: 型は定義済みだが、生成・読み取り箇所がコード上一切ない（ARCHITECTURE が「ゆいが読む」と言っている重要型）。
- **`Issue.summary` の生成器なし**: 型は定義され MOCK_ISSUES に手書き mock があるが、「会話の節目で書き出す」ロジックが `issue-chat-mock.ts` に存在しない。**ゆい→葵 だけでなく 葵→ゆいの基本動線が手書きデータでしか動いていない**。

### 過剰設計

- `TutorCard` 7 種類、`TutorRightPaneAction` 8 種類、`RightPaneView` 8 種類が独立 type で持たれている。`TutorRightPaneAction` と `RightPaneView` はほぼ 1:1 対応なので派生関数で十分。
- `IssueOccurrence.triggerMessageId` と `Issue.triggerMessageId` の二重持ち。

### Phase 6 (Claude API) リスク

1. **コンテキスト境界が「型」しかない**: 「サマリーのみ読む」と原則表明しているが、コード上「ゆいに何を渡すか」を制御するレイヤーが存在しない。Phase 6 着手前に **「ゆいへの入力アダプタ」関数 `buildTutorContext(args)`** を切るべき。
2. **`TutorStep` ステートマシン 13 state が scripted 都合で増殖**: LLM 駆動になると不要。**Phase 6 で全捨てになる前提のコード 800 行超**。state machine 層と発話層に分離すべき。
3. **`searchTutorThreads` の grep 検索**: Phase 6 で「埋め込み検索に置換」予定だが、Supabase 移行（Phase 7）との依存順序が曖昧。
4. **フランク敬語の prompt 化が困難**: TUTOR_PERSONA.description 1 段落だけで LLM がトーンを安定再現するには few-shot 必須。tutor-mock.ts の発話を **canonical sample として prompt に流す導線がない**。

### 改善提案 5 つ（仕様レベル）

1. **types.ts に Phase 3 拡張型を仮実装で commit**: 6 型を ARCHITECTURE.md の定義そのままで追加 + `mock-data.ts` に 1 件ずつ mock → tutor-mock の excavation で `MOCK_HANDOFFS.push(...)` 実行 → 「申し送りしておくよ」が嘘でなくなる。
2. **`SessionEndReason` を `"manual"` 単独に縮退**: dead code 約 30 行 + 認知負荷削減。
3. **`tutor-mock.ts` を state machine 層と発話生成層に分離**: 前者を Phase 6 で捨て、後者を few-shot examples として system prompt に投入。
4. **`Issue.summary` 自動生成 mock を `issue-chat-mock.ts` に追加**: `buildIssueSummary(issue): string` の簡易実装で疎通させ、Phase 6 では LLM call に差し替え。
5. **`buildTutorContext(args)` 入力アダプタ skeleton を `tutor-mock.ts` に新設**: `args: { issues, sessions, reflections, handoffs }` を受けて「ゆいに渡してよい情報のサブセット」だけを返す pure function。**これがないと Phase 6 で `Issue.chatThread` を全部 prompt に入れる事故が必ず起きる**。

---

## 2. UX/UI レビュー

### 強み

- ゆい先生のペルソナ設計（`TUTOR_PERSONA.description` + `TUTOR-ROLE.md`）がコーチング軸で一貫
- 復元テストの即時 ✓/× フィードバック（`MindMapReconstructionTest.tsx`）が「待たされない・恥かかない」設計
- パステル emoji `TopicChip`（`topic-display.tsx`）が中2 女子向け
- `NotePane.tsx` の課題追加が `border-dashed` で控えめ → 「分からない」を出す心理コスト低
- `LearnHeader.tsx` の 3 状態タイマー + `aria-live` が ARCHITECTURE 改訂と整合

### ナビゲーション問題

- **`/tutor` の HubMenu + quickReplies + composer の 3 系統並行** → 「次に何を押せばいいか」が散らかる。HubMenu は折りたためる「+ メニュー」トリガにして、デフォルト quickReplies だけ見せる方が良い
- **`/learn` から `/tutor` への戻り動線が「学習を終了」 1 つしかない** → 「ゆいに相談（セッション継続）」と「終了」を分けるべき
- `NotePane.tsx` の「一覧へ」リンクが `/tutor?view=issues` に飛ぶが、`/learn` を抜けることが文言から読めない

### 視覚階層

- **フォントが極小すぎる**: `text-[9px]` / `text-[10px]` / `text-[11px]` が **135 箇所**。中2 がタブレットで読むには小さい
- `LearnHeader` の breadcrumb が最大 5 段 + ページ範囲 + タイマー + 終了ボタン = **横幅 1 行に 12 要素**、wrap 指定なしで横スクロール or 切れる
- `ScheduleDashboard` の `md:grid-cols-[1fr_360px]` がペイン内 embed で効かない（ブレイクポイントは window 基準）
- 色が散漫: TopicChip 9 色 + SourceBadge (blue/purple) + ScheduleTaskTypeIcon 4 色 + emerald + destructive = **一画面に 5 系統の色アクセント**

### トーン/語り口

- ゆいの直接発話は良い。**周辺 UI のコピーが事務的で乖離**:
  - `DialogPane.tsx`: 「このノードのチャットはまだありません。音声で話しかけて始めよう。」→「まだここでは話してないね。話しかけてみて!」
  - `NotePane.tsx`: 「AI と会話を始めると…」→「葵先生と話すと、ノートが育つよ」
  - `SessionEndDialog.tsx`: 「お疲れさま、今日の学習を記録しました」→「お疲れさま!今日もよくがんばったね」

### タブレット対応

- タッチターゲットが小さい: `size-7` / `size-6` (24-28px) → HIG 推奨 44px に
- **音声入力ボタンが disabled** で見える → README/PHILOSOPHY で「話すだけ」を約束してるのに最初に触る画面で押せない、裏切り感
- `MindMapReconstructionTest` の DnD: `PointerSensor` / `TouchSensor` の登録なし、タブレット指ドラッグ要検証
- `/learn` 4 ペイン + `/tutor` 2 ペインがタブレット縦持ちで破綻、レスポンシブ縦積み版が必要

### アクセシビリティ・可読性

- **`whitespace-pre-wrap` のメッセージで markdown 風 `**...**` を生で表示** → ゆいの発話で `**今から…**` がそのまま `**` 付きで表示される
- `text-[9px]` は物理的に判読困難
- Mic ボタンに a11y label なし

### 改善提案（実装難易度付き）

| # | 規模 | 提案 |
|---|---|---|
| 1 | 小 1h | **メッセージ markdown 化**: `TutorMessageBubble` の text div を `ReactMarkdown` に。これだけで劇的に読みやすくなる |
| 2 | 小 30m | **過小フォント整理**: `text-[9px]` 撤去、最低 `text-[10px]` / 本文 `text-sm` 保証 |
| 3 | 小 1h | **HubMenu の折りたたみ**: quickReplies が出てる時は自動で隠す |
| 4 | 中 2-3h | **タッチターゲット 44px 底上げ**: `size="icon"` のデフォルトを `size-9` に |
| 5 | 中 2h | **/tutor 2 ペインのタブレット縦持ち対応**: view 切替で全画面化 |
| 6 | 小 1h | **周辺コピーをゆい/葵語彙に統一**: 「AI」「システム」を消す |
| 7 | 中 2h | **「ゆいに相談」と「学習を終了」を分離**: paused にしてから遷移、戻り URL 保持 |
| 8 | 小 30m | **音声ボタンを Phase 8 まで非表示**: env var で gate |
| 9 | 小 1h | **タッチドラッグ保証**: `MindMapReconstructionTest` に PointerSensor + TouchSensor 明示 |

---

## 3. コーチング有効性レビュー（最も辛口）

### 総評

> **設計は本物のコーチング、実装はフォーム入力のチューター**

ドキュメントの設計純度は極めて高いが、コーチングの核心（本人の中から引き出す）が scripted な分岐で代替されてしまっている。

### 良く実装されてる箇所

- `buildInitialTutorThread("ending")` の開口発話 ito19 canonical script は秀逸（「ふわっと → 具体化」を明言してから喋らせる）
- `ending-vent` の毎ターン累積サマリーループは本物のメタ認知促進
- `excavation` state の検知 → 1 ターン深掘り → Issue + TutorHandoff 派生（仕組みは）
- IssueChat の `trigger-message-quote` カードで言い逃げさせない設計
- 「教えるのは葵、ゆいは振る」境界が型レベルで分離（**構造的勝利**）

### コーチング原則に届いてない箇所

- **GROW は完全に名ばかり**: `reflection-yesterday → school → mood → questions → plan` は**フォーム入力**であって、G/R/O/W の動的な行き来は一切ない
- **承認が観察ベースになっていない**: `reflection-school` の `"覚えといたよ、それ。あとで復習タスクに入れとくね。"` は本人が何を言っても同じ返事 = **観察してない**
- **「掘り起こし」が 1 ターンで終わる**: `userInput.slice(0, 40)` を課題タイトル化して終了 = **書き起こし**であって深掘りではない
- **`LongTermGoal` / `ReflectionLog` 実装ゼロ** → コーチング契約のない状態 = **コーチングですらない**

### 欠けてるコーチング機能

- 複数時間軸ゴール設定（月初・週初・試験前）
- 気分パターン把握（週次グラフ、「最近月曜が辛そう」検知）
- 成功体験のお祝い（マイルストーン承認）
- 成長の認知（「先月の自分と今月の自分」対比）
- 質問の深掘り（Socratic ladder）
- 沈黙の活用（「3 秒待つ」演出、即レス過剰）
- **mirroring (本人の言葉を引用して返す)** — 一切なし

### 「ただのチューター」に堕落するリスク

- hub action が `lower.includes("課題")` で即右ペインを開く → コーチング的には「何で課題見たいの?」と一拍置くべき
- **Phase 6 で `TUTOR_PERSONA.description` だけで「教えない」を保つのは極めて危険** → Opus は親切なので本人の質問に答えてしまう確率高。**system prompt のハードガード + post-generation classifier が必要**
- 復元テストの即時 ✗ は「赤フラッシュ + 取り消し線」で**評価的**に見える → PHILOSOPHY「ミスは学びの瞬間」と矛盾
- IssueChat の「分かった!」ボタンが**ヘッダ常設で 1 ターンも会話せず押せる** → 暗記モードの「終わったことにする」を許容、**ファインマン式と矛盾**

### 中2 女子という user 特性

- **反抗的・口数少ない時の分岐ゼロ**: `"ふつう"` を `"うん、いい感じ。"` で流す。中2 の `"ふつう"` は **「話したくない」のシグナル**
- **「めんどくさい」「無理」「やる気ない」検知ゼロ** → 学習開始の代わりに「5 分だけ?」「眺めるだけ?」を出すべき
- **拒否権がない**: quickReplies に「**今日はパス**」「**飛ばす**」が一切ない
- **親への共有がブラックボックス**: 中2 女子は親に何が伝わるか神経質、「親には共有されないよ」のサインが必要

### ito19 さん本人の経験 vs AI 現状

ito19 さんがスタッフ相談で実際にやってる事のうち、AI がまだ再現できてないもの:

1. **「当てはめる」フェーズ** — 過去の似たケースを引っ張る能動性なし
2. **「指揮者として束ねる」フェーズ** — 箇条書きで並べるだけ、テーマ抽出してない
3. **沈黙と「で?」** — 毎回テンプレ応答で空白を埋めてしまう
4. **「お前に決めさせる」プレッシャー** — quickReplies で本人に決めさせてない

### 改善提案（優先順位付き）

| # | 優先 | 提案 |
|---|---|---|
| 1 | **最高** | **「教えない」ハードガード** を scripted で実装。`detectTeachingRequest()` で「○○って何?」「教えて」「答えは?」検知 → 必ず葵に振る。Phase 6 で system prompt + classifier の二重ガード化 |
| 2 | **最高** | **`ReflectionLog` 型 + 朝の振り返り永続化**。session 内消滅ではコーチング契約が成立しない。最低 7 日分積む + 「先週月曜と比べて気分どう?」発話可能に |
| 3 | 高 | **掘り起こしを 3-5 ターンの ladder にする**。`excavation-deepen-1/2/3`、「具体化」→「比較」→「適用」の 3 段固定 |
| 4 | 高 | **mirroring の強制**。応答生成時に userInput から名詞・形容詞抽出 → テンプレに `{userPhrase}` 埋め込み。「観察ベースの承認」感が激変 |
| 5 | 高 | **拒否権・縮小オプションを全儀式に追加**。`reflection-*` 各 state の quickReplies に「今日はパス」「短くして」を含める |
| 6 | 中 | **「分かった!」ボタンの条件付き活性化**。`turnCount < 3` または learner messageCount < 2 では disable + tooltip「自分の言葉で 1 回説明してから押してね (ファインマン式)」 |
| 7 | 中 | **テーマ抽出による「束ねる」発話**。`ending-vent` の最終サマリーで items を clustering → 「全部、不定詞の用法の見分けで詰まってるね」を追加 |
| 8 | 中 | **成長対比の週次儀式**。`WeeklyGoal` 実装と同時に「先週課題 vs 今週クリア」を見せる `WeeklyGrowthCard` |
| 9 | 中 | **「で?」の沈黙活用**。`[3 秒考えて答える]` チップ、ドット表示 |
| 10 | 低 | **マイルストーン承認**。初課題クリア / 連続 7 日 / 復元テスト全問正解 で `MilestoneCard`、観察ベース承認 |

### 結論（一行）

> **設計は本物のコーチング、実装はフォーム入力のチューター**。最重要は (1) 教えないハードガード、(2) ReflectionLog 永続化でコーチング契約を成立、(3) 掘り起こし 1 → 3 ターン化 — この 3 つで「ただのチューターに堕落するリスク」が一気に下がる。

---

## 全体の最重要 3 つ

3 つのレビューを横断して見ると、即着手すべき最重要は:

1. **`**markdown**` レンダリング** (UX レビュー #1、1h)
   - 既存ゆい発話の太字・リストが現状 `**...**` のまま表示されてる
   - `TutorMessageBubble` の text を `ReactMarkdown` に置換するだけ
   - 投資対効果が圧倒的

2. **「教えない」ハードガード** (コーチング #1、1-2h)
   - Phase 6 で Claude が親切に教えてしまうリスクを潰す前提工事
   - `detectTeachingRequest()` を scripted で実装、tutor-mock の冒頭で判定 → 必ず葵に振る
   - 構造的に最重要、Phase 6 着手前に必須

3. **Phase 3 拡張型 6 個を types.ts に追加 + mock 1 件ずつ** (仕様 #1、コーチング #2、2-3h)
   - ARCHITECTURE と code の乖離を解消
   - `ReflectionLog` / `LongTermGoal` / `WeeklyGoal` / `GoalReview` / `TutorHandoff` / `NodeComprehension`
   - 「申し送りしておくよ」が嘘でなくなる
   - その上で Phase 3c-3g の実装が乗る土台ができる

これらは独立に実装可能、合計半日〜1 日で全部入る規模。
